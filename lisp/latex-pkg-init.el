;;; Code:
;(when (not (ignore-errors (el-get-executable-find "auctex")))
;  (error "Can't find auctex"))

(add-to-list 'my:el-get-packages 'flymake)

(provide 'latex-pkg-init)
;;; latex-pkg-init.el ends here
